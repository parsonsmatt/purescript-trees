{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "clean": "rimraf bower_components && rimraf output && rimraf .psa-stash",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "12.4.2",
    "purescript": "0.12.5",
    "purescript-psa": "0.7.3",
    "rimraf": "^2.5.4"
  }
}
