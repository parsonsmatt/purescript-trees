{
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "build": "pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^10.0.0",
    "purescript-psa": "^0.4.0",
    "rimraf": "^2.5.4"
  }
}
